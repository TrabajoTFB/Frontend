import React from "react";
import CartComp from "../components/layout/CartComp";

const Cart: React.FC = () => {
    return (
        <div className="min-h-screen">
            <CartComp />
        </div>
    );
};

export default Cart;